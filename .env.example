# Reference names for local/hosted configuration.
# ASP.NET Core maps double underscores to nested configuration keys.
# The repository does not automatically load this file.

ConnectionStrings__CareTrack=
AzureAd__TenantId=
AzureAd__ClientId=
Cors__AllowedOrigins__0=http://localhost:4200
ASPNETCORE_FORWARDEDHEADERS_ENABLED=

# Supply only to the guarded reset process; clear it immediately afterward.
CARETRACK_DEMO_DB_CONNECTION_STRING=
